# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Cursor / skills CLI (régénérables localement)
.cursor/
.agents/
skills-lock.json
Untitled

# Claude Code (settings locaux — chemins machine, ne pas exposer)
.claude/

# Fichiers temporaires pipeline (données prospects, PII, URLs internes)
tmp_*.json
tmp_*.sh
tmp_*.csv
insert_pipeline_*.sh

# Migrations Supabase (contiennent données personnelles pré-peuplées)
# Exécute-les manuellement via le SQL editor Supabase
supabase/
