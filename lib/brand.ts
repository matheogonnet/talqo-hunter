/** Favicon officiel Talqo (source distante). */
export const TALQO_FAVICON_URL = 'https://talqo.fr/favicon.ico'

/**
 * Favicon servi par Next (`app/favicon.ico` + `public/favicon.ico`, même fichier).
 * Next priorise `app/favicon.ico` — ne pas laisser le favicon Vercel par défaut du template.
 */
export const TALQO_FAVICON_PATH = '/favicon.ico'

/** Incrémenter après remplacement du .ico pour invalider le cache navigateur. */
export const TALQO_FAVICON_VERSION = 'talqo-1'
